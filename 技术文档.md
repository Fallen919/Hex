# 海克斯棋项目技术文档

## 文件结构说明

### 核心模块

#### 1. game_core.py - 游戏核心逻辑

python

```
class GameState:
    """
    海克斯棋游戏状态管理
    支持C++后端加速或纯Python实现
    
    主要功能：
    - 棋盘状态管理
    - 走法合法性验证
    - 胜负判断
    - 快速随机模拟
    
    关键方法：
    make_move(): 执行走法
    winner(): 判断胜者
    get_legal_moves(): 获取合法走法
    random_playout(): 随机模拟到终局
    """
```

#### 2. mcts_agent.py - MCTS代理

python

```
class MCTSAgent:
    """
    UCT MCTS代理
    使用Upper Confidence Bound for Trees算法
    
    搜索流程：
    1. 选择：从根节点开始，使用UCB公式选择子节点
    2. 扩展：到达未完全扩展的节点时，添加新子节点
    3. 模拟：从扩展节点进行随机模拟
    4. 反向传播：将模拟结果回传更新路径上所有节点
    
    参数：
    - exploration: 探索参数（默认1.41）
    - think_time: 思考时间（秒）
    """

class RAVEAgent(MCTSAgent):
    """
    RAVE增强的MCTS代理
    使用Rapid Action Value Estimation算法
    
    改进点：
    - 记录所有走法作为第一手时的统计
    - 结合AMAF统计和UCT统计
    - 更快的价值估计收敛
    """
```

#### 3. Hex_cnn_model.py - CNN模型

python

```
class HexCNN(nn.Module):
    """
    海克斯棋CNN模型架构
    
    网络结构：
    输入层：3×11×11（红棋、蓝棋、当前玩家）
    初始卷积：3→128通道
    12个残差块：每块包含卷积层+批归一化+跳跃连接
    双头输出：
      - 策略头：预测每个位置的走法概率
      - 价值头：评估局面胜率（-1到1）
    
    训练方法：
    1. 使用MCTS生成的高质量数据监督学习
    2. 多任务学习：策略损失 + 价值损失
    3. 经验回放：存储历史数据重复训练
    """
```

#### 4. Hex_cnn_integration.py - CNN集成模块

python

```
class CNNAgent:
    """
    CNN代理类，可与MCTS代理互换使用
    
    主要功能：
    - 加载预训练模型
    - 执行神经网络推理
    - 与游戏状态交互
    - 训练新模型
    
    训练流程：
    1. 生成MCTS数据（高质量对局）
    2. 训练神经网络（监督学习）
    3. 保存和转换模型
    4. 评估模型性能
    """
```

#### 5. opening_book.py - 开局库

python

```
class OpeningBook:
    """
    海克斯棋开局库管理系统
    
    功能：
    - 提供不同棋盘尺寸的开局建议
    - 根据对手走法动态调整策略
    - 支持开局库的保存和加载
    - 权重随机选择机制
    
    数据结构：
    - 第1手：红方优先占据中心
    - 第2手：蓝方根据红方走法应对
    - 第3手：红方构建连接结构
    """
```

#### 6. game_record.py - 棋谱管理

python

```
class GameRecord:
    """
    海克斯棋棋谱管理系统
    
    支持格式：
    - 标准海克斯棋谱格式
    - SGF格式（标准围棋格式）
    - 纯文本格式
    
    功能：
    - 记录每一步走法
    - 保存比赛信息（选手、时间、地点）
    - 棋谱导入导出
    - 棋谱回放
    """
```

#### 7. hex_gui.py - 图形界面

python

```
class Gui:
    """
    海克斯棋图形用户界面
    
    主要面板：
    1. 游戏选项：棋盘大小、思考时间、AI算法选择
    2. 棋谱管理：记录、保存、加载、回放
    3. 开局库：开局走法推荐和配置
    4. CNN模型：深度学习模型训练和加载
    
    功能：
    - 人机对战和AI对AI对战
    - 悔棋功能
    - 计时器
    - 走法历史显示
    """
```

#### 8. main.py - 主程序入口

python

```
"""
项目启动入口
初始化GUI界面并启动主循环
"""
```

#### 9. train_cnn_mcts.py - CNN训练脚本

python

```
"""
独立的CNN训练脚本
提供标准的训练流程：
1. 生成MCTS训练数据
2. 训练神经网络
3. 保存最佳模型
"""
```

## 关键算法详解

### 1. 蒙特卡洛树搜索（MCTS）

#### UCT算法

python

```
UCB公式：exploit + explore
exploit = wins / visits  # 胜率
explore = C * sqrt(log(parent_visits) / visits)  # 探索项
C = 1.41  # 探索常数
```

#### RAVE算法

python

```
RAVE值：rave_wins / rave_visits
beta = sqrt(k / (3*visits + k))  # 权重系数
最终评分 = (1-beta)*UCT + beta*RAVE
```

### 2. 卷积神经网络（CNN）

#### 输入表示

text

```
通道0：红棋位置（1表示有棋）
通道1：蓝棋位置（1表示有棋）
通道2：当前玩家（全1表示红方，全0表示蓝方）
```

#### 损失函数

python

```
总损失 = 策略损失 + 2.0 * 价值损失
策略损失 = 交叉熵(模型预测, MCTS分布)
价值损失 = MSE(模型评估, 实际结果)
```

### 3. 训练流程

#### 阶段1：数据生成

python

```
# 使用RAVE MCTS生成高质量数据
步骤：
1. MCTS思考2秒/步，获得访问次数分布
2. 将分布归一化为概率目标
3. 记录棋盘状态、策略目标、当前玩家
4. 游戏结束后，根据胜者分配价值标签
```

#### 阶段2：模型训练

python

```
# 监督学习训练CNN
for epoch in range(100):
    for batch in 经验池:
        前向传播 → 计算损失 → 反向传播 → 更新权重
    调整学习率（自适应）
    保存检查点（每5轮）
```

#### 阶段3：模型评估

python

```
# 自我对弈评估
让CNN与MCTS对战，统计胜率
或让CNN与不同AI对战，比较性能
```

## 使用指南

### 1. 环境要求

bash

```
Python 3.12
```

### 2. 运行程序

bash

```
# 启动GUI界面
python main.py

# 训练CNN模型
python train_cnn_mcts.py
```

### 3. GUI操作说明

#### 游戏选项面板

* 思考时间：设置AI每步思考时间（1-20秒）

* 选择算法：UCT、RAVE、CNN三种AI算法

* 当前玩家：设置人机对战或AI对AI

* 重置棋盘：开始新游戏

* 生成走法：让AI走一步

#### 棋谱管理面板

* 保存SGF：保存棋谱为SGF格式

* 加载SGF：加载已有棋谱

* 导出TXT：导出为文本格式

* 棋谱回放：浏览历史走法

#### 开局库面板

* 保存开局库：保存自定义开局库

* 加载开局库：加载开局库文件

* 推荐走法：查看当前局面推荐

#### CNN模型面板

* 加载模型：加载预训练CNN模型

* 训练模型：开始训练新模型

* 评估模型：测试模型性能

* 转换模型：转换检查点为纯模型

## 训练指南

### 1. 快速开始（使用预训练模型）

python

```
# 在GUI中：
1. 切换到"CNN模型"面板
2. 点击"加载模型"
3. 选择预训练模型文件（*.pth）
4. 切换算法为"CNN"
5. 开始对弈
```

### 2. 从头训练CNN模型

python

```
# 方法1：使用训练脚本
python train_cnn_mcts.py

# 方法2：在GUI中训练
1. 切换到"CNN模型"面板
2. 设置训练参数：
   - 训练局数：100-500
   - 学习率：0.001
   - 批次大小：32
   - 训练轮数：10-100
3. 点击"训练模型"
4. 选择保存路径
5. 等待训练完成
```

### 3. 训练参数说明

| 参数     | 推荐值   | 说明            |
| :----- | :---- | :------------ |
| 训练局数   | 200   | 局数越多，数据质量越高   |
| MCTS时间 | 2.0秒  | 每步思考时间，影响数据质量 |
| 学习率    | 0.001 | 初始学习率，会自动调整   |
| 批次大小   | 32    | 每次训练的样本数      |
| 训练轮数   | 50    | 训练迭代次数        |

## 文件格式说明

### 1. 模型文件格式

python

```
# 检查点格式（*.pth）
{
    'model_state_dict': 模型权重,
    'optimizer_state_dict': 优化器状态,
    'epoch': 训练轮数,
    'loss': 训练损失,
    'board_size': 棋盘大小
}

# 纯模型格式（*.pth）
只包含模型权重，用于推理
```

### 2. 棋谱文件格式

python

```
# SGF格式（标准）
(;GM[Hex]SZ[11]PB[红方]PW[蓝方]
;AB[aa][bb]AW[cc][dd])

# 文本格式
海克斯棋谱
==============
比赛：友谊赛
红方：玩家1
蓝方：玩家2
1. 红 A1
2. 蓝 B2
```

### 3. 开局库格式（JSON）

json

```
{
    "board_size": 11,
    "max_opening_moves": 3,
    "openings": {
        "0": {
            "best_moves": [[5,5], [5,6]],
            "weights": [0.7, 0.3],
            "description": "红方第1手"
        }
    }
}
```

## 性能优化

### 1. C++加速

* 游戏状态管理：使用C++实现，速度提升10倍

* 快速随机模拟：纯C++模拟，无需Python交互

* MCTS搜索：关键循环使用C++优化

### 2. 内存管理

* 经验回放缓冲区：固定大小，防止内存溢出

* 梯度裁剪：防止梯度爆炸

* 检查点保存：定期保存，防止训练中断丢失

### 3. 计算优化

* 批量推理：CNN一次处理多个状态

* 并行模拟：MCTS模拟可并行化

* 缓存机制：重复状态缓存计算结果

## 故障排除

### 常见问题

#### 1. CNN模型加载失败

python

```
# 可能原因：
1. 模型文件损坏
2. 模型与棋盘大小不匹配
3. PyTorch版本不兼容

# 解决方法：
1. 重新训练模型
2. 检查棋盘大小设置
3. 确保PyTorch版本一致
```

#### 2. C++模块无法加载

python

```
# 可能原因：
1. 未编译C++模块
2. 系统缺少依赖库
3. Python版本不匹配

# 解决方法：
1. 运行 make 编译
2. 安装 g++ 编译工具链
3. 使用Python 3.8+
```

#### 3. 训练过程崩溃

python

```
# 可能原因：
1. 内存不足
2. 梯度爆炸
3. 数据损坏

# 解决方法：
1. 减少批次大小
2. 启用梯度裁剪
3. 检查训练数据
```

### 调试工具

python

```
# 启用详细日志
import logging
logging.basicConfig(level=logging.DEBUG)

# 测试导入
python -c "import hex_cpp; import game_core"

# 检查设备
python -c "import torch; print(torch.cuda.is_available())"
```

## 扩展开发

### 1. 添加新AI算法

python

```
class NewAgent(MCTSAgent):
    def __init__(self):
        super().__init__()
        self.name = "NewAgent"
    
    def _mcts_search(self, think_time):
        # 实现新算法
        pass
```

### 2. 修改网络架构

python

```
class CustomCNN(nn.Module):
    def __init__(self, board_size=11):
        super().__init__()
        # 自定义网络层
        self.custom_layer = nn.Conv2d(3, 64, 5, padding=2)
    
    def forward(self, x):
        # 自定义前向传播
        x = self.custom_layer(x)
        return policy, value
```

### 3. 扩展开局库

python

```
# 添加新的开局序列
opening_book.add_opening_sequence(
    sequence=[(5,5), (4,6), (6,4)],
    weight=1.0,
    description="三角开局"
)
```

## 项目结构

text

```
hex-game/
├── main.py                    # 主程序入口
├── hex_gui.py                 # 图形界面
├── game_core.py               # 游戏核心逻辑
├── mcts_agent.py              # MCTS代理
├── opening_book.py            # 开局库系统
├── game_record.py             # 棋谱管理
├── cnn/                       # CNN模块文件夹
│   ├── __init__.py            # CNN包初始化
│   ├── Hex_cnn_model.py       # CNN模型定义
│   ├── Hex_cnn_integration.py # CNN集成模块
├── train_cnn_mcts.py          # 训练脚本          
├── hex_cpp/                   # C++加速模块（可选）
│   ├── hex_cpp.cpp
│   ├── hex_cpp.h
│   └── setup.py
├── models/                    # 预训练模型
├── opening_libs/              # 开局库文件
├── training_logs/             # 训练日志
├── checkpoints/               # 训练检查点
└── DOCUMENTATION.md           # 详细技术文档
```

##

## 更新日志

### v1.0.0（当前版本）

* 完整实现海克斯棋游戏

* 集成三种AI算法（UCT、RAVE、CNN）

* 图形用户界面

* 棋谱管理和回放功能

* 开局库系统

* C++加速支持

### 后续计划

* 强化学习训练

* 在线对弈功能

* 移动端适配

* 更多AI算法集成

